/**
 * Regenerate event-payload TypeScript types from
 * `packages/contracts/asyncapi/events.asyncapi.yaml`.
 *
 * Run with: pnpm gen:types
 */

async function main(): Promise<void> {
  // eslint-disable-next-line no-console
  console.log('[gen:types] placeholder — wire AsyncAPI generator once event schemas stabilise.');
}

void main();
