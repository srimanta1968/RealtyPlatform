/**
 * Placeholder unit-test scaffold for `services/user-service`.
 *
 * Real assertions land in Task 2-4 (email verification, login, sessions).
 * Run with `pnpm --filter @kiana/user-service test` once jest + ts-jest are wired.
 */

describe('user-service: auth domain', () => {
  it.todo('registers a new user with email + password');
  it.todo('rejects duplicate email registrations');
  it.todo('verifies password and issues a session token');
});
