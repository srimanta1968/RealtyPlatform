openapi: 3.1.0
info:
  title: Kiana CRM Service
  version: 0.1.0
  description: Pipeline, tasks, SLAs, follow-ups, workflows. Owns crm_db.
servers:
  - url: http://localhost:4013
paths:
  /api/workflows/_status:
    get:
      operationId: crmServiceStatus
      summary: Scaffold-only health echo
      tags: [meta]
      responses:
        '200':
          description: OK
