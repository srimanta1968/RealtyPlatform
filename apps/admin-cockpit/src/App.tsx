/**
 * Admin cockpit shell — scaffold-only placeholder.
 * Routes for lead inbox, pipeline, workflow monitoring, etc. land in
 * subsequent tasks (Tasks 7, 12).
 */
export function App(): JSX.Element {
  return (
    <main style={{ fontFamily: 'system-ui', padding: '2rem' }}>
      <h1>Kiana Admin Cockpit</h1>
      <p>Operator cockpit scaffold. Real screens land in Phase 1 follow-up tasks.</p>
    </main>
  );
}
